{
  "name": "live-chat-server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "npm i && npm run build:client && tsc",
    "build:client": "cd ../client && npm run build"
  },
  "dependencies": {
    "express": "4.17.1",
    "socket.io": "4.8.1"
  },
  "devDependencies": {
    "@types/express": "5.0.0",
    "ts-node": "10.9.2",
    "typescript": "5.7.3"
  }
}
