{
  "name": "live-chat-server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "npm run build:client && ts-node src/index.ts",
    "build": "tsc",
    "build:client": "cd ../client && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  }
}
